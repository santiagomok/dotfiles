set breakpoint pending on
set print elements 0
set auto-load safe-path /

# dashboard displayed after the stack frame is changed using up, down, or frame commands
define hookpost-up
    dashboard
end

define hookpost-down
    dashboard
end

define hookpost-frame
    dashboard
end
